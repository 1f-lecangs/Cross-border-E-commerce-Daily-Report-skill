{
  "name": "push-daily-report-skill",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sanitized Codex Skill for generating and optionally publishing daily RSS briefs",
  "scripts": {
    "check": "node --check push-daily-report/scripts/push_daily_report.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
